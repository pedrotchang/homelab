apiVersion: v1
kind: ConfigMap
metadata:
  name: pai-memory-config
data:
  # PostgreSQL connection (Data cluster)
  POSTGRES_HOST: "pai-pg.seyzahl.com"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "pai"
  POSTGRES_COLLECTION_NAME: "memories"
  # Ollama connection (Tachtit cluster - internal)
  OLLAMA_HOST: "http://ollama.ollama.svc.cluster.local:11434"
  OLLAMA_MODEL: "phi3"
  # LLM provider configuration
  LLM_PROVIDER: "ollama"
  # Server settings
  HOST: "0.0.0.0"
  PORT: "3000"
