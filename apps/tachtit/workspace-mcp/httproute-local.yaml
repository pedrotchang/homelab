apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: workspace-mcp-local
  annotations:
    external-dns.alpha.kubernetes.io/hostname: workspace-mcp.local.seyzahl.com
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: default
  hostnames:
    - workspace-mcp.local.seyzahl.com
  rules:
    - backendRefs:
        - name: workspace-mcp
          port: 8080
