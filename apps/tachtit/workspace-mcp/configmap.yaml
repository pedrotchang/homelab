apiVersion: v1
kind: ConfigMap
metadata:
  name: workspace-mcp-configmap
data:
  PORT: "8080"
  HOST: "0.0.0.0"
  LOG_LEVEL: "info"
  GOOGLE_OAUTH_REDIRECT_URI: "https://workspace-mcp.local.seyzahl.com/oauth2callback"
