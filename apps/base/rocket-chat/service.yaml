apiVersion: v1
kind: Service
metadata:
  name: rocket-chat
  labels:
    app: rocket-chat
    app.kubernetes.io/name: rocket-chat
    app.kubernetes.io/instance: rocket-chat
    app.kubernetes.io/component: web
spec:
  selector:
    app: rocket-chat
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
