apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisper-cache-pvc
  labels:
    app: whisper
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
