apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kmcp
  namespace: kmcp-system
spec:
  interval: 30m
  dependsOn:
    - name: kmcp-crds
  chart:
    spec:
      chart: kmcp
      sourceRef:
        kind: HelmRepository
        name: kmcp
        namespace: kmcp-system
      interval: 12h
