apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gluteun-vpn-config
  namespace: servarr
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-kv-store
    kind: ClusterSecretStore
  data:
    - secretKey: WIREGUARD_PRIVATE_KEY
      remoteRef:
        key: wireguard-private-key
