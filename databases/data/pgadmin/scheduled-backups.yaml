apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pgadmin-db-data
spec:
  immediate: true
  schedule: "0 0 3 * * *"
  backupOwnerReference: cluster
  cluster:
    name: pgadmin-db-data-cnpg-v0
