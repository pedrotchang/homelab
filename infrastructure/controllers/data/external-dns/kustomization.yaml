apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/external-dns

configMapGenerator:
  - name: external-dns-values
    files:
      - values.yaml
    behavior: merge
