provider:
  name: cloudflare

policy: upsert-only

sources:
  - service

env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-dns-api-token
        key: cloudflare-dns-api-token
