apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: blog
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: blog
  filterTags:
    # Match 7-character git SHA tags (hex only)
    pattern: '^[a-f0-9]{7}$'
  policy:
    alphabetical:
      order: desc
